/*package it.polimi.ingsw.PSP38.client.GUIComponents;

import javax.swing.*;
import javax.swing.border.Border;
import java.awt.*;

public class FirstCardSelectionWindow extends JFrame {
    public static final int WIDTH = 800;
    public static final int HEIGHT = 600;

    public FirstCardSelectionWindow(){
        super();
        setSize(WIDTH, HEIGHT);
        setTitle("Santorini");
        setLayout(new BorderLayout());

        JPanel headerPanel = new JPanel();
        JLabel headerLabel = new JLabel("Please select three cards");
        headerPanel.add(headerLabel);
        add(headerPanel, BorderLayout.NORTH);

        JPanel cardListPanel = new JPanel();

        add(cardListPanel, BorderLayout.WEST);
    }
}*/
